<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="PdfCounter.Views.SaveJobEditor"
    x:CompileBindings="False">
     
  <!-- Put your item editor UI here (the grid with Name/X/Y/W/H etc.) -->
  <Border Padding="8" MaxHeight="122">
      <Grid ColumnDefinitions="80,*"
            RowDefinitions="Auto"
            ColumnSpacing="6" RowSpacing="6">

        <TextBlock VerticalAlignment="Center" Grid.Column="0" Grid.Row="0" Text=" Job Name"/>
        <TextBox  Grid.Column="1" Grid.Row="0" Text="{Binding JobName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <!-- <TextBlock VerticalAlignment="Center" Grid.Column="0" Grid.Row="1" Text="X"/>
        <NumericUpDown Value="{Binding X, Mode=TwoWay}" Grid.Column="1" Grid.Row="1" Minimum="0" Maximum="1000" Increment="0.1"  />

        <TextBlock VerticalAlignment="Center" Grid.Column="0" Grid.Row="2" Text="Y"/>
        <NumericUpDown Value="{Binding Y, Mode=TwoWay}" Grid.Row="2" Grid.Column="1" Minimum="0" Maximum="1000" Increment="0.1"  />

        <TextBlock VerticalAlignment="Center" Grid.Column="0" Grid.Row="3" Text="W"/>
        <NumericUpDown Value="{Binding Width, Mode=TwoWay}" Grid.Row="3" Grid.Column="1" Minimum="0" Maximum="2000" Increment="0.1" />

        <TextBlock VerticalAlignment="Center" Grid.Column="0" Grid.Row="4" Text="H"/>
        <NumericUpDown Value="{Binding Height, Mode=TwoWay}" Grid.Row="4" Grid.Column="1" Minimum="0" Maximum="2000" Increment="0.1" />

        <TextBlock VerticalAlignment="Center" Grid.Column="0" Grid.Row="5" Text="Group"/>
        <NumericUpDown Value="{Binding GroupNumber, Mode=TwoWay}" Grid.Column="1" Grid.Row="5" Minimum="0" Maximum="2000" Increment="1" /> -->

        <!-- <StackPanel Margin="0 7 0 7" Grid.Row="6" Grid.Column="1" HorizontalAlignment="Left" Orientation="Horizontal">
          <TextBlock Margin="0 7 5 0" Text="First Page Identifier"/>
         <CheckBox IsChecked="{Binding IsFirstPageIdentifier, Mode=TwoWay}">
          <CheckBox.IsEnabled>
            <MultiBinding Converter="{StaticResource FpiEnabledConverter}">
              current item’s IsFirstPageIdentifier 
              <Binding Path="IsFirstPageIdentifier"/>
             
              <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="DataContext.AnyFirstPageIdentifier"/>
            </MultiBinding>
          </CheckBox.IsEnabled>
        </CheckBox>
        </StackPanel>
        <StackPanel Margin="0 7 0 7" Grid.Row="6" Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
          <TextBlock HorizontalAlignment="Right" Margin="0 7 5 0" Text="Bottom Left"/>
          <CheckBox IsChecked="{Binding IsBottomLeft, Mode=TwoWay}" />
        </StackPanel> -->
      </Grid>
  </Border>
</UserControl>
